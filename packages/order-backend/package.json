{
  "name": "@usdt007/order-backend",
  "version": "0.1.0",
  "description": "Cloudflare Worker that collects user intents from the USDT007 frontend and serves them to fill agents",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --name order-backend",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250313.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.14.0"
  }
}
