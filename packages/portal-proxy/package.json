{
  "name": "@wdk-hackathon/portal-proxy",
  "version": "0.1.0",
  "description": "Cloudflare Worker proxy for the 1delta portal API (hides API key from frontend)",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250313.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.14.0"
  }
}
